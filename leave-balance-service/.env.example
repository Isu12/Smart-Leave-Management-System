# .env.example
# Purpose: Template for environment variables. Demonstrates which configuration variables are required for the service to run.

PORT=5000
MONGO_URI=mongodb://localhost:27017/leave_balance_db
